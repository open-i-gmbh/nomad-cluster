[Unit]
Description=Consul-template Service
After=network.target consul.service

[Service]
Type=simple
User={{ consul_user }}
ExecStart={{ consul_install_dir }}/consul-template
Restart=on-abort

[Install]
WantedBy=multi-user.target
